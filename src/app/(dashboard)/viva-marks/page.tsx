"use client";
import ConstructionPage from "@/components/ui/ConstructionPage";
/**
 * Renders the client-side page displaying the "VIVA MARKS" construction placeholder.
 *
 * @returns The React element for the ConstructionPage configured with title "VIVA MARKS".
 */
export default function Page() {
  return <ConstructionPage title="VIVA MARKS" />;
}
