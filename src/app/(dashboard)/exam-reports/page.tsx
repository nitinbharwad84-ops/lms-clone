"use client";
import ConstructionPage from "@/components/ui/ConstructionPage";
/**
 * Renders the client-side Exam Reports page as a ConstructionPage placeholder.
 *
 * @returns The React element for the page — a ConstructionPage with title "EXAM REPORTS".
 */
export default function Page() {
  return <ConstructionPage title="EXAM REPORTS" />;
}
